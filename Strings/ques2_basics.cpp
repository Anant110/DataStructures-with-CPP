#include<bits/stdc++.h>
using namespace std;


int main(){
    // string s1;
    // cin>>s1;
    // sort(s1.begin(),s1.end());
    // int n=s1.size();
    // string result="";
    // for(int i=n-1;i>=0;i--){
    //     result+=s1[i];
    // }
    // cout<<result;

    // sort(s1.begin(),s1.end(),greater<int>());
    // cout<<s1;

    //Maximum character print
    // string s1="toisiss";
    // unordered_map<char,int> mpp;
    // int n=s1.size();
    // for(int i=0;i<n;i++){
    //     mpp[s1[i]]++;
    // }
    // int maxi=0;
    // char m;
    // for(auto it:mpp){
    //     if(it.second>maxi){
    //         maxi=max(maxi,it.second);
    //         m=it.first;
    //     }
    // }
    // if(maxi>1)cout<<m<<" "<<maxi;
    // if(maxi==1 && n>1) cout<<"frequency is same";
    // if(n==1) cout<<m<<" "<<maxi;
}